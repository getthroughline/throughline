---
description: Drop a diary-style note into your Throughline memory (no schema, feeds reflection)
argument-hint: <the note>
---

Save this as a journal entry in my self's memory, verbatim: "$ARGUMENTS"

Use the `journal` tool. No evidence ceremony, no confirmation needed — it saves immediately and
reflection distills it later. Just confirm it's saved in one short line.
