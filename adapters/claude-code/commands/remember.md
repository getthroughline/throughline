---
description: Explicitly capture something to your Throughline memory
argument-hint: <what to remember>
---

I want to record this to my self's memory: "$ARGUMENTS".

Draft a grounded candidate event with `propose_events` (evidence = this conversation), show me a
one-line summary, and call `confirm_events` only after I approve. Record observable facts only.
