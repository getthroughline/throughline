---
description: Delete a Throughline self and all its data
argument-hint: <self name>
---

I want to delete the self "$ARGUMENTS". First confirm the exact name back to me and warn that this
deletes all its data and cannot be undone. Only after I explicitly say yes, call `delete_self`.
