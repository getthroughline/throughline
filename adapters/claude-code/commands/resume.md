---
description: Resume your Throughline self (optionally switch to another)
argument-hint: [self name]
---

Call the `resume` tool. If I gave a name in "$ARGUMENTS", pass it so we switch to that self.
Confirm which self is now active, and remind me it applies to new sessions.
