{
  "name": "throughline",
  "version": "0.7.0",
  "description": "Throughline continuity for Claude Code: self-context injection, deterministic write-back, tiered capture, journal, recall. Cloud-first; self-host optional.",
  "mcpServers": {
    "throughline": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp/server.mjs"
      ]
    }
  }
}