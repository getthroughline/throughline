{
  "name": "throughline",
  "version": "0.7.5",
  "description": "Throughline continuity for Claude Code: self-context injection, journal-first low-friction capture, reflection, recall. Cloud-first; self-host optional.",
  "mcpServers": {
    "throughline": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp/server.mjs"
      ]
    }
  }
}